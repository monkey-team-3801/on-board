import React from "react";

type Props = {
    sessionId: string;
};

export const FileContainer: React.FunctionComponent<Props> = (props: Props) => {
    return (
        <div>
            <h1>document test</h1>
        </div>
    );
};
