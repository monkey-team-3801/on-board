import React from "react";

type Props = {
    user: string;
    text: string;
};

export const ShortAnswerDisplayContainer = (props: Props) => {
    return (
        <div>
            <h1>test</h1>
        </div>
    );
};
